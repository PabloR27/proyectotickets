{% extends 'base.html.twig' %}

{% block title %}Edit Usuarios{% endblock %}

{% block body %}
    <style>
.vich-image>div{
display: none;
}
</style>
    <h1>Edit Usuarios</h1>
<a href="{{path('app_forgot_password_request')}}">Cambiar contraseña</a>
    {{ include('usuarios/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_usuarios_index') }}">back to list</a>

    {{ include('usuarios/_delete_form.html.twig') }}
{% endblock %}
